-- [createTestTable onerr=resume]
DROP TABLE ESQL_SECRET_TEST;

CREATE TABLE ESQL_SECRET_TEST (
  A INT NOT NULL,
  B CHAR(1000),
  C VARCHAR2(1000),
  D TIMESTAMP,
  E int
);

-- [insertSecret]
INSERT INTO ESQL_SECRET_TEST(A, B, C)
VALUES (##, ##, ##);

-- [updateSecret]
UPDATE ESQL_SECRET_TEST
SET B = #2#
WHERE C = #1#

-- [selectSecret]
SELECT B, C
FROM ESQL_SECRET_TEST
WHERE A = ##


