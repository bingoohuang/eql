-- import org/n3r/eql/SimpleTest.eql *TestTable

-- [addBean]
INSERT INTO EQL_TEST VALUES(##, 'J', ##, ##, 104);

-- [updateBean]
UPDATE EQL_TEST
SET C = #2#
WHERE A = #1#

-- [getBean]
SELECT C
FROM EQL_TEST
WHERE A = ##

